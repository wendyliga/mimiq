
name: Upload Binary

on:
  push:
    tags:
      - '*'

jobs:
  upload_binary:
    name: Upload Binary
    runs-on: macos-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Build Binary
      uses: make build
    - name: Upload binaries to release
      uses: svenstaro/upload-release-action@v1-release
      with:
        repo_token: ${{ secrets.GITHUB_TOKEN }}
        file: .build/release/mimiq
        asset_name: mimiq
        tag: ${{ github.ref }}
        overwrite: true